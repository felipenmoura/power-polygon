module.exports= {
	// Use this address to validade logins(such as personas)
	// if you changed the port or hosted PPS in your own
	// server, remember to change it
	serverDomain: 'http://localhost:8081/'
};